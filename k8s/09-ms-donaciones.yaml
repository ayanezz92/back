apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-donaciones
  namespace: sanos-salvos
  labels:
    app: ms-donaciones
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-donaciones
  template:
    metadata:
      labels:
        app: ms-donaciones
    spec:
      containers:
        - name: ms-donaciones
          image: ayanez92/sanos-salvos-ms-donaciones:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8085
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/db_donaciones"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka-server:8761/eureka/"
          resources:
            requests:
              cpu: "150m"
              memory: "384Mi"
            limits:
              cpu: "500m"
              memory: "768Mi"
          readinessProbe:
            tcpSocket:
              port: 8085
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            tcpSocket:
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 20
            failureThreshold: 6
---
apiVersion: v1
kind: Service
metadata:
  name: ms-donaciones
  namespace: sanos-salvos
  labels:
    app: ms-donaciones
spec:
  selector:
    app: ms-donaciones
  ports:
    - port: 8085
      targetPort: 8085
  type: ClusterIP
