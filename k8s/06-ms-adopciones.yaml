apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-adopciones
  namespace: sanos-salvos
  labels:
    app: ms-adopciones
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-adopciones
  template:
    metadata:
      labels:
        app: ms-adopciones
    spec:
      containers:
        - name: ms-adopciones
          image: ayanez92/sanos-salvos-ms-adopciones:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/db_adopciones"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka-server:8761/eureka/"
          resources:
            requests:
              cpu: "150m"
              memory: "384Mi"
            limits:
              cpu: "500m"
              memory: "768Mi"
          readinessProbe:
            tcpSocket:
              port: 8082
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            tcpSocket:
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 20
            failureThreshold: 6
---
apiVersion: v1
kind: Service
metadata:
  name: ms-adopciones
  namespace: sanos-salvos
  labels:
    app: ms-adopciones
spec:
  selector:
    app: ms-adopciones
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP
